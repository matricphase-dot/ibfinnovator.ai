'use client';import Link from 'next/link';import {Bookmark,Clock,ArrowUpRight,Sparkles} from 'lucide-react';import {useState} from 'react';
export default function ProjectCard({p,compact=false}:{p:any,compact?:boolean}){const [saved,setSaved]=useState(false);return <article className="bg-white border border-slate-200 rounded-2xl p-5 hover:-translate-y-1 hover:shadow-xl hover:shadow-indigo-100/50 transition-all"><div className="flex items-start"><div className="w-11 h-11 rounded-xl grid place-items-center text-white font-black text-sm" style={{background:p.color}}>{p.title.slice(0,2).toUpperCase()}</div><div className="ml-3"><h3 className="font-extrabold text-[15px]">{p.title}</h3><p className="text-xs text-slate-500 mt-1">{p.domain} · {p.stage}</p></div><button onClick={()=>setSaved(!saved)} className={`ml-auto p-2 rounded-lg ${saved?'bg-violet-100 text-violet-600':'text-slate-400 hover:bg-slate-50'}`}><Bookmark size={17} fill={saved?'currentColor':'none'}/></button></div><p className="text-sm text-slate-600 leading-6 mt-4 line-clamp-2">{p.desc}</p><div className="flex flex-wrap gap-1.5 mt-4">{p.skills.map((s:string)=><span key={s} className="pill bg-slate-100 text-slate-600">{s}</span>)}</div><div className="flex items-center gap-3 mt-5 pt-4 border-t border-slate-100"><span className="pill bg-emerald-50 text-emerald-700"><Sparkles size={12}/>{p.match}% match</span><span className="text-[11px] text-slate-500 flex items-center gap-1"><Clock size={12}/>{p.commit}</span><Link href={`/projects/${p.id}`} className="ml-auto text-violet-600"><ArrowUpRight size={18}/></Link></div></article>}
