'use client';import {FileText,Send,X} from 'lucide-react';import {FormEvent,useState} from 'react';import toast from 'react-hot-toast';export default function ApplyToProject({projectId}:{projectId:string}){const [open,setOpen]=useState(false),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget),r=await fetch('/api/applications',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({project_id:projectId,cover_letter:f.get('cover_letter'),resume_url:f.get('resume_url')||undefined})}),d=await r.json();setLoading(false);if(r.ok){toast.success('Application submitted');setOpen(false)}else toast.error(d.error||'Unable to apply')}return <><button onClick={()=>setOpen(true)} className="btn btn-primary w-full"><FileText size={16}/>Apply to this project</button>{open&&<div className="fixed inset-0 z-[100] bg-black/75 backdrop-blur-sm grid place-items-center p-4"><form onSubmit={submit} className="w-full max-w-lg bg-[#111827] border border-white/10 rounded-2xl p-6 text-left"><div className="flex"><div><p className="text-[9px] tracking-widest text-cyan-300 font-bold">EXPRESS INTEREST</p><h2 className="text-xl font-black mt-1">Introduce yourself</h2></div><button type="button" onClick={()=>setOpen(false)} className="ml-auto"><X/></button></div><label className="block text-sm font-bold mt-6">Why are you a strong fit? *<textarea required minLength={30} maxLength={3000} name="cover_letter" className="field mt-2 min-h-36" placeholder="Explain your relevant skills, motivation, availability and what you can contribute."/></label><label className="block text-sm font-bold mt-4">Resume or portfolio URL<input type="url" name="resume_url" className="field mt-2" placeholder="https://…"/></label><button disabled={loading} className="btn btn-primary w-full mt-6"><Send size={16}/>{loading?'Submitting…':'Submit application'}</button></form></div>}</>}
