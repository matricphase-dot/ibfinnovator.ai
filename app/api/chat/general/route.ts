import {NextRequest,NextResponse} from 'next/server';import {createClient,requireUser} from '@/lib/supabase/server';import {z} from 'zod';export async function GET(req:NextRequest){const s=await createClient();const before=req.nextUrl.searchParams.get('before');let q=s.from('messages').select('*,sender:profiles!sender_id(id,name,avatar_url)').eq('room_type','GENERAL').order('created_at',{ascending:false}).limit(50);if(before)q=q.lt('created_at',before);const {data,error}=await q;if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json((data||[]).reverse())}export async function POST(r:Request){try{const {supabase,user}=await requireUser();const {content}=z.object({content:z.string().trim().min(1).max(5000)}).parse(await r.json());const {data,error}=await supabase.from('messages').insert({sender_id:user.id,room_type:'GENERAL',content}).select('*,sender:profiles!sender_id(id,name,avatar_url)').single();if(error)throw error;return NextResponse.json(data,{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
