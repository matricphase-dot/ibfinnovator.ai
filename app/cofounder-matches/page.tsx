import AppShell from '@/components/AppShell';import {people} from '@/lib/data';import {HeartHandshake,Sparkles} from 'lucide-react';export default function Page(){return <AppShell><div className="max-w-5xl mx-auto p-8"><span className="pill bg-pink-50 text-pink-600"><HeartHandshake size={14}/>Co-founder mode</span><h1 className="text-3xl font-black mt-3">Build with someone aligned.</h1><p className="text-slate-500 mt-2">Matches based on vision, values, commitment and complementary strengths.</p><div className="grid md:grid-cols-3 gap-5 mt-8">{people.map(p=><div className="bg-white border border-slate-200 rounded-2xl p-6 text-center"><span className="w-16 h-16 mx-auto rounded-2xl text-white grid place-items-center font-black" style={{background:p.color}}>{p.initials}</span><h2 className="font-black mt-4">{p.name}</h2><p className="text-sm text-slate-500">{p.role}</p><span className="pill bg-violet-50 text-violet-700 mt-4"><Sparkles size={12}/>{p.score}% aligned</span><div className="flex justify-center gap-1 mt-4">{p.skills.map(s=><span className="pill bg-slate-100 text-slate-600">{s}</span>)}</div><button className="btn btn-primary w-full mt-6">View compatibility</button></div>)}</div></div></AppShell>}
