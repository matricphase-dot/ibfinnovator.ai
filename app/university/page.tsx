'use client';import AppShell from '@/components/AppShell';import {GraduationCap,Loader2} from 'lucide-react';import {useEffect,useState} from 'react';export default function University(){const [d,setD]=useState<any>(null);useEffect(()=>{fetch('/api/university').then(r=>r.json()).then(setD)},[]);return <AppShell><div className="max-w-6xl mx-auto p-8"><p className="text-[10px] tracking-[.2em] text-cyan-300 font-bold">UNIVERSITY PARTNER PORTAL</p><h1 className="text-3xl font-black mt-2">Campus opportunities</h1>{!d?<Loader2 className="animate-spin text-cyan-300 mx-auto mt-24"/>:!d.membership?<div className="py-20 text-center"><GraduationCap className="mx-auto text-slate-600" size={44}/><h2 className="font-bold mt-4">No university linked</h2><p className="text-sm text-slate-500 mt-2">University membership requires a matching verified institution email.</p></div>:<><p className="text-slate-500 mt-2">{d.membership.university.name} · {d.students.length} verified members</p><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4 mt-8">{d.projects.map((p:any)=><a href={`/projects/${p.id}`} className="project-cyber-card"><h2 className="font-bold">{p.title}</h2><p className="text-sm text-slate-500 mt-2">{p.domain} · {p.stage}</p><div className="flex gap-1 mt-4">{p.required_skills.slice(0,3).map((s:string)=><span className="tech-chip">{s}</span>)}</div></a>)}</div></>}</div></AppShell>}
