'use client';import AppShell from '@/components/AppShell';import {FormEvent,useState} from 'react';import {Loader2,Plus,X} from 'lucide-react';import {useRouter} from 'next/navigation';
export default function NewProject(){const router=useRouter();const [skills,setSkills]=useState<string[]>([]),[skill,setSkill]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const body={title:f.get('title'),description:f.get('description'),domain:f.get('domain'),stage:f.get('stage'),required_skills:skills,engagement_type:f.get('engagement_type'),commitment_hours:Number(f.get('commitment_hours')),duration_weeks:Number(f.get('duration_weeks'))};const r=await fetch('/api/projects',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const d=await r.json();setLoading(false);if(!r.ok)return setError(typeof d.error==='string'?d.error:'Please check every field');router.push(`/projects/${d.id}`)}function add(){const s=skill.trim();if(s&&!skills.includes(s))setSkills([...skills,s]);setSkill('')}return <AppShell><div className="max-w-3xl mx-auto p-5 md:p-8"><p className="text-xs tracking-widest font-black text-violet-600">FOUNDER WORKSPACE</p><h1 className="text-3xl font-black mt-2">Create a real project</h1><p className="text-slate-500 mt-2">Your project will be saved to Supabase and immediately enter matching.</p><form onSubmit={submit} className="bg-white border border-slate-200 rounded-3xl p-7 mt-7 space-y-5"><label className="block text-sm font-bold">Project title<input required name="title" minLength={3} className="field mt-2" placeholder="e.g. EcoTrack AI"/></label><label className="block text-sm font-bold">Description<textarea required name="description" minLength={20} className="field mt-2 min-h-32" placeholder="What are you building and why does it matter?"/></label><div className="grid sm:grid-cols-2 gap-4"><label className="text-sm font-bold">Domain<input name="domain" className="field mt-2" placeholder="Climate Tech"/></label><label className="text-sm font-bold">Stage<select name="stage" className="field mt-2"><option>IDEA</option><option>MVP</option><option>BETA</option><option>REVENUE</option><option>FUNDED</option></select></label></div><label className="block text-sm font-bold">Required skills<div className="flex gap-2 mt-2"><input value={skill} onChange={e=>setSkill(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'){e.preventDefault();add()}}} className="field" placeholder="Type a skill and press Enter"/><button type="button" onClick={add} className="btn btn-secondary"><Plus size={16}/></button></div><div className="flex flex-wrap gap-2 mt-2">{skills.map(s=><span className="pill bg-violet-50 text-violet-700">{s}<button type="button" onClick={()=>setSkills(skills.filter(x=>x!==s))}><X size={12}/></button></span>)}</div></label><div className="grid sm:grid-cols-3 gap-4"><label className="text-sm font-bold">Engagement<select name="engagement_type" className="field mt-2"><option>EQUITY</option><option>STIPEND</option><option>VOLUNTEER</option></select></label><label className="text-sm font-bold">Hours/week<input required name="commitment_hours" type="number" min="1" max="80" className="field mt-2" defaultValue="10"/></label><label className="text-sm font-bold">Duration (weeks)<input required name="duration_weeks" type="number" min="1" className="field mt-2" defaultValue="12"/></label></div>{error&&<p className="p-3 bg-red-50 text-red-700 rounded-xl text-sm">{error}</p>}<button disabled={loading||!skills.length} className="btn btn-primary w-full">{loading?<Loader2 size={17} className="animate-spin"/>:null}Publish project</button></form></div></AppShell>}
