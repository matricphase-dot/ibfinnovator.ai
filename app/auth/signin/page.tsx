'use client';
import Link from 'next/link';import {Github,Sparkles,Loader2} from 'lucide-react';import {FormEvent,useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function SignIn(){const [loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);try{const s=createClient();const {error}=await s.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});if(error)throw error;location.href='/dashboard'}catch(e:any){setError(e.message)}finally{setLoading(false)}}async function oauth(provider:'google'|'github'){try{const s=createClient();await s.auth.signInWithOAuth({provider,options:{redirectTo:`${location.origin}/auth/callback`}})}catch(e:any){setError(e.message)}}return <div className="min-h-screen grid lg:grid-cols-2"><div className="p-6 md:p-12 flex flex-col"><Link href="/" className="flex gap-2 items-center font-black text-xl"><span className="w-9 h-9 rounded-xl bg-violet-600 text-white grid place-items-center"><Sparkles size={18}/></span>IBF</Link><div className="w-full max-w-md m-auto py-10"><h1 className="text-3xl font-black">Welcome back</h1><p className="text-slate-500 mt-2">Sign in to your real IBF workspace.</p><div className="grid grid-cols-2 gap-3 mt-7"><button onClick={()=>oauth('google')} className="btn btn-secondary"><b className="text-lg">G</b> Google</button><button onClick={()=>oauth('github')} className="btn btn-secondary"><Github size={18}/>GitHub</button></div><div className="flex items-center gap-3 my-6 text-xs text-slate-400"><i className="h-px bg-slate-200 flex-1"/>OR EMAIL<i className="h-px bg-slate-200 flex-1"/></div><form onSubmit={submit}><label className="text-sm font-bold">Email address<input name="email" required className="field mt-2 mb-4" placeholder="you@example.com" type="email"/></label><label className="text-sm font-bold">Password<input name="password" required minLength={8} className="field mt-2" placeholder="••••••••" type="password"/></label>{error&&<p className="mt-4 p-3 bg-red-50 text-red-700 rounded-xl text-sm">{error}</p>}<button disabled={loading} className="btn btn-primary w-full mt-6">{loading?<Loader2 className="animate-spin" size={17}/>:null}Sign in</button></form><p className="text-center text-sm text-slate-500 mt-6">New to IBF? <Link href="/auth/signup" className="text-violet-600 font-bold">Create an account</Link></p></div></div><div className="hidden lg:flex dark-bg p-14 text-white items-end"><div className="max-w-lg"><span className="text-7xl text-violet-300">“</span><blockquote className="text-3xl font-bold leading-snug">IBF helped me find a team that cared as much about the problem as I did.</blockquote><p className="mt-6 text-violet-200">Maya · Product designer</p></div></div></div>}
